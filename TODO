* add record.store(t, cx)
** use in record test

* add record isStored test

* add model tests
** isStored/isModified

* add index support
* add sequence support

        try {
            Class.forName("org.h2.Driver");
        } catch (final ClassNotFoundException e) {
            throw new RuntimeException(e);
        }